{
  "name": "pocketprofits",
  "version": "1.0.0",
  "description": "A selfhosted Poker & Casino Bankroll tracker app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "mkdir -p dist && find . -path ./dist -prune -o -path ./.github -prune -o -path ./node_modules -prune -o -type f -exec cp --parents {} dist/ \\;",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "dumbdateparser": "^1.2.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
